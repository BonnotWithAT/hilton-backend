const resolvers = require('../resolvers');

const mockContext = {
  
}

const testReservation = {
  name: "Chappy Buster",
  hotelName: "Hilton Dallas",
  arrivalDate: "May 30, 2019",
  departureDate: "May 31, 2019"
}

describe('[Query.allReservations]', () => {
  it('Is able to hit the reservations list', () => {

  });
});